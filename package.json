{
  "name": "dsh-desktop",
  "productName": "DSH Desktop",
  "version": "0.3.0",
  "private": true,
  "description": "DeepSeek Harness 桌面工作台：Electron 原生壳 + 内嵌 harness 运行时（dsh-desktop-bridge 桥接）",
  "main": "dist/main/index.cjs",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "link:dev": "node scripts/dev-link.mjs",
    "setup:runtime": "node scripts/setup-runtime.mjs",
    "make:icons": "node scripts/make-icons.mjs",
    "dist:win": "node scripts/build.mjs && node scripts/make-icons.mjs && node scripts/setup-runtime.mjs && electron-builder --win nsis",
    "dist:win:portable": "node scripts/build.mjs && node scripts/make-icons.mjs && node scripts/setup-runtime.mjs && electron-builder --win portable"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "electron-updater": "^6.6.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "ws": "^8.21.3"
  }
}
