feat+docs+ci: 完成遗留 2/4/5 —— README 更新机制同步、CI 缓存提速、更新卡跨重启保留

2) README「更新机制」段落后：同步新流程（右上角进度卡 + 点「安装更新并重启」按钮/
   通知安装；下载完退出后下次启动仍会重新提示）。

5) 更新卡不再「一次性」：
   - settings 新增 pendingUpdateVersion：下载完成即持久化；
   - 启动 4s 后 maybeResumePendingUpdate：若存在待装版本且非当前版本 → 重新显示
     「安装更新」卡片 + 按钮 + 通知；已到该版本则自动清理；
   - requestUpdateInstall 兜底：重启后 electron-updater 内存态丢失时，先 checkNow
     重新确认缓存下载，确认完成后自动进入安装确认框（autoInstallAfterDownload）。

4) CI 提速：三个构建 job（win + mac×2）增加 actions/cache 缓存 resources/dsh-runtime，
   key = os + arch + hashFiles(setup-runtime, bridge pkg)；命中时 setup-runtime 走
   增量路径（跳过 20-30 分钟 npm install，仅 prune+tar，约 1 分钟）。